dbname: sportspoll-users
username: greendev
password: greendev920704Sad
params:
  dialect: sqlite
  storage: ./database/users-sequelize.sqlite3
  operatorAliases:
    $eq: Op.eq
    $ne: Op.ne
    $gte: Op.gte
    $gt: Op.gt
    $lte: Op.lte
    $lt: Op.lt
    $or: Op.or
